source ~/dotfilesp/common/.bashrc

# if [ -n "$SSH_CONNECTION" ]; then
#     echo 'SSH CONNECTION'
# elif [ "${TMUX+set}" ]; then
#     echo '----------'
# else
#     # open tmux by default
#     tmux -2
#     echo "Exiting... (ENTER to cancel)"
#     read -t 0.2 || exit
# fi
